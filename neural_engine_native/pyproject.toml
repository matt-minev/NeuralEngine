[build-system]
requires = ["scikit-build-core>=0.10", "pybind11>=2.12"]
build-backend = "scikit_build_core.build"

[project]
name = "neural-engine-native"
version = "0.1.0"
requires-python = ">=3.9"

[tool.scikit-build]
wheel.packages = ["neural_engine_native"]
cmake.build-type = "Release"
